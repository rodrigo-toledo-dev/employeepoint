import React, { Component } from 'react'
import { Text, View } from 'react-native'

import { Body } from '../GeneralStyles';

export default class SetEmployee extends Component {
  render() {
    return (
      <Body />
    )
  }
}
